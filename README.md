# SoundCloud Desktop Clone

A powerful desktop application for SoundCloud, built with Electron and React. High-performance, bypass-ready, and feature-rich.

## 🚀 Что нового в версии 1.0.5

### 🛠 Сетевой движок нового поколения
- **Переход на Native Electron Net**: Основной сетевой слой переписан с `axios` на нативный модуль `electron.net`. 
  - **Производительность**: Снижено потребление оперативной памяти и нагрузка на CPU за счет использования оптимизированного движка Chromium.
  - **Стабильность**: Нативная интеграция с системными настройками прокси Windows.
- **Поддержка SOCKS5**: Теперь приложение полноценно поддерживает SOCKS5 прокси (включая авторизацию) для всех API-запросов.

### 🛡 Обход блокировок и DPI
- **Совместимость с GoodbyeDPI**: Благодаря нативному движку, приложение автоматически работает через запущенные в системе средства обхода блокировок (GoodbyeDPI, Zapret и др.).
- **Smart TLS Fingerprinting**: Запросы теперь выглядят как обычный трафик браузера Сhrome, что затрудняет блокировку со стороны провайдеров.

### 🔄 Автоматизация и Self-Healing
- **Auto Client-ID Refresh**: Реализована система автоматического обновления `client_id`. Если SoundCloud блокирует текущий ключ, приложение само получит новый в фоновом режиме и продолжит работу без прерывания музыки.
- **Умные повторы**: Автоматическое повторение запросов при временных сбоях сети или прокси.

### 🧹 Оптимизация интерфейса и логов
- **Чистая консоль**: Удален лишний отладочный вывод. В терминале отображаются только важные системные сообщения.
- **Ускоренный поиск**: Результаты поиска теперь подгружаются быстрее благодаря переиспользованию соединений (Keep-Alive) нативного движка.

## Основные возможности

- Поиск и воспроизведение любых треков и плейлистов.
- Локальное избранное и история прослушивания.
- Полноценный плеер с поддержкой Crossfade и эквалайзером.
- Поддержка различных типов прокси (HTTP, HTTPS, SOCKS5).
- Кэширование треков для экономии трафика.

## Технологии

- **Framework**: Electron + React.
- **Networking**: Electron Native `net` (основной), Axios (загрузка файлов).
- **Audio**: Howler.js.
- **Proxy Support**: `socks-proxy-agent`, `http-proxy-agent`.

## Установка и запуск

1. Установите зависимости: `npm install`.
2. Запустите в режиме разработки: `npm run dev`.
3. Соберите установщик: `npm run dist`.